# OpenTailcat 1.3.0

First 1.3 line. Development signing when `OPENTAILCAT_RELEASE_*` is unset. Do not
overclaim production privacy or leak-free beyond the evidence below.

## Highlights

- UI: home subtitle and Settings about no longer say “Development test / not
  leak-free”; home reads “Private gateway for Tailcat”.
- Version **1.3.0** / versionCode **30**.
- README rewritten as a short product page (screenshots to follow).
- Phase 8 **emulator** dual-capture uplink analyze (`analyze-uplink.sh`) **PASS**
  (exit 0). Physical-phone **Connect** works with a live token.
- Carries forward 1.2.14 live TUN rates in the status notification / telemetry.

## Evidence (honest)

- Emulator + WARP-path dual capture: official `scripts/phase8/analyze-uplink.sh`
  completed with exit 0 for this tree’s Phase 8 DNS-ignore fix (`83e7c73`).
- Phone: Connect succeeds with Always-on + block-without-VPN.
- Not claimed: production signing, physical multi-interface leak certification
  beyond the emulator dual-capture pass, or “leak-free” marketing language.

## Build

```bash
./gradlew assembleDevelopment
```

APKs: `app/build/outputs/apk/development/app-{arm64-v8a,x86_64}-development.apk`
