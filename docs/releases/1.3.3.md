# OpenTailcat 1.3.3

Fixes a networking corruption that could leave a stale `200.x` resolver/exit after a failed `Connect`.

## Bug

In 1.3.2 and earlier (including the 1.2.14 checkpoint in `handoff.md`) a failed
`prepare` — e.g. `gateway handshake failed: context deadline exceeded` after
dialing a `200.111.5.10:443`/`[2001:db8::1]:443` DERP or a user `FORCED_RESOLVER`
`200.160.0.8:53` — left `globalCore.pendingDNS` (`core-engine/main.go:214`)
set from the `TailcatVpnService.kt:126` `updateNetworkState` that ran **before**
`prepare`. `core-engine/lifecycle.go:219` `abandonPrepare()` cleared
`sess/state` but not `pendingDNS`. The next `AttachTun`
(`lifecycle.go:256`) therefore re-used the stale `FORCED_RESOLVER` via
`bridge.go:97` `SetDNSConfig`, forcing port-53 through `Client.DialUDP` to the
stale `200.x:53` (`netstack_proxy.go:resolveDNSDestination`). With the 5s
`udpProbeTimeout` (`bridge.go:697`) latched `tcpOnly=true`, non-DNS UDP was
also dropped until the 30s `udpReprobeInterval` (`bridge.go:701`) or an explicit
`Stop()` cleared `pendingDNS` (`lifecycle.go:408`). The window was typically
30s + the 15s `HealthStale` grace (`service/EngineHealth.kt:13`) — i.e. “for some
time” after a single failed tap. The UI could also keep showing
`Exit IP: 200.x` (`TelemetryCard.kt:57` used `transportType != UNKNOWN`).

## Fix

* `core-engine/lifecycle.go:225` `abandonPrepare` now `pendingDNS.Store(nil)`.
* `app/src/main/java/com/tailcat/vpn/ui/screens/home/components/TelemetryCard.kt:60`
  now `isLiveRunning(nowSec)` (`NetworkMetrics.kt:50`, 15s) so a `FAILED`/
  stale-health tunnel never shows a stale `Exit IP: 200.x`.

## Verification

* `go test -race ./...`, `testDebugUnitTest`, `lintDebug`, `VpnStartupInstrumentedTest`
  (synthetic `tc…` with embedded `200.111.5.10` DERP) now goes
  `CONNECTING (10s) → DISCONNECTED` with `lastError=gateway handshake failed:
  context deadline exceeded`, `networkMetrics=UNKNOWN`/`tunnelEgressIp=null`,
  `ip route` no `200.` TUN route, and a subsequent profile with `1.1.1.1`
  correctly uses `1.1.1.1`.
* AAR built reproducibly with Go 1.27.1, NDK 29.0.14206865, 16 KB ELF
  (`aa0fa1bdda9ae102d3ef7a7153c2d1ceca3f5165c8c707e8b490d97997a75999`).

## Build

```bash
./gradlew assembleDevelopment
```

APKs: `app/build/outputs/apk/development/app-{arm64-v8a,x86_64}-development.apk`

Version **1.3.3** / versionCode **33**. Still development-signed when
`OPENTAILCAT_RELEASE_*` is unset.
